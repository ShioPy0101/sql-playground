{
  "number": 42,
  "slug": "table-design-create-customers",
  "title": "[テーブル設計][確認しよう] customers テーブルを作成する",
  "statement": "顧客を保存する customers テーブルを作成してください。列は id, name, email, created_at。id を INTEGER PRIMARY KEY、name と created_at を NOT NULL とし、email は NULL を許可します。",
  "constraints": [
    {
      "name": "主キー",
      "value": "id は INTEGER PRIMARY KEY にします"
    },
    {
      "name": "必須列",
      "value": "name と created_at は TEXT NOT NULL にします"
    },
    {
      "name": "任意列",
      "value": "email は TEXT として、NOT NULL は付けません"
    }
  ],
  "csv": "",
  "starterSql": "CREATE TABLE customers (\n  id INTEGER PRIMARY KEY,\n  name TEXT NOT NULL,\n  email TEXT,\n  created_at TEXT NOT NULL\n);",
  "solutionSql": "CREATE TABLE customers (\n  id INTEGER PRIMARY KEY,\n  name TEXT NOT NULL,\n  email TEXT,\n  created_at TEXT NOT NULL\n);",
  "checkSql": "SELECT name AS column_name, UPPER(type) AS type, \"notnull\" AS not_null, pk FROM pragma_table_info('customers') ORDER BY cid;",
  "expectedCsv": "column_name,type,not_null,pk\nid,INTEGER,0,1\nname,TEXT,1,0\nemail,TEXT,0,0\ncreated_at,TEXT,1,0\n",
  "tests": [
    {
      "name": "スキーマ確認",
      "csv": "",
      "expectedCsv": "column_name,type,not_null,pk\nid,INTEGER,0,1\nname,TEXT,1,0\nemail,TEXT,0,0\ncreated_at,TEXT,1,0\n"
    }
  ]
}
